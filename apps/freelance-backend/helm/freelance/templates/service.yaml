apiVersion: v1
kind: Service
metadata:
  name: {{ include "freelance.fullname" . }}
  labels:
    {{- include "freelance.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "freelance.selectorLabels" . | nindent 4 }}
